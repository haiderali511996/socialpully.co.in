{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd video_downloader && python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn video_downloader.wsgi:application --bind 0.0.0.0:$PORT --workers 1 --timeout 300"
  }
}
