# Web framework
Django==5.2.8
djangorestframework==3.16.1
django-cors-headers==4.9.0

# Serving
gunicorn==23.0.0
whitenoise==6.11.0

# Database
dj-database-url
psycopg2-binary
# On cPanel (MySQL, no compiler) drop psycopg2-binary and use this instead:
# PyMySQL==1.1.2

# Caching (used when REDIS_URL is set)
django-redis==6.0.0
redis==7.1.0

# Celery integration (registered in INSTALLED_APPS)
celery==5.6.2
django-celery-beat==2.8.1
django-celery-results==2.6.0

# Video extraction
yt-dlp==2025.12.8
# Powers browser-impersonation (settings.YTDLP_ENABLE_IMPERSONATION), which
# makes yt-dlp's requests carry a real Chrome TLS/HTTP fingerprint instead of
# Python's default one. 0.7.3 (the previous pin) is NOT supported by yt-dlp
# 2025.12.8 -- confirmed it raises "Only curl_cffi versions 0.5.10, 0.10.x,
# 0.11.x, 0.12.x, 0.13.x are supported" at import time, silently disabling
# impersonation. 0.13.0 is the newest version in that supported range.
curl_cffi==0.13.0
pycryptodome==3.23.0
requests==2.32.5

tzdata==2025.2
